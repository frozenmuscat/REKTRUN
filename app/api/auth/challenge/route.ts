import { init,db,json } from "../../../../lib/server";import { isAddress } from "viem";
export async function POST(req:Request){try{const {address}=await req.json() as {address:string};if(!isAddress(address))return json({error:"Invalid address"},400);await init();const nonce=crypto.randomUUID();const exp=Date.now()+5*60_000;await db().prepare("INSERT OR REPLACE INTO auth_nonces(address,nonce,expires_at) VALUES(?,?,?)").bind(address.toLowerCase(),nonce,exp).run();const message=`REKTRUN login\n\nWallet: ${address.toLowerCase()}\nChain: Robinhood Chain Testnet (46630)\nNonce: ${nonce}\nExpires: ${new Date(exp).toISOString()}\n\nThis signature does not trigger a transaction or cost gas.`;return json({message})}catch{return json({error:"Challenge unavailable"},500)}}
