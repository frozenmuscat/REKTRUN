import { init,db,json,token,verifyWallet } from "../../../../lib/server";
export async function POST(req:Request){try{const {address,message,signature}=await req.json() as {address:string;message:string;signature:`0x${string}`};await init();const a=address.toLowerCase();const [row]=await db()<[{nonce:string;expires_at:string}]>`SELECT nonce,expires_at FROM auth_nonces WHERE address=${a}`;if(!row||Number(row.expires_at)<Date.now()||!message.includes(`Nonce: ${row.nonce}`)||!message.includes("Chain: Robinhood Chain Testnet (46630)"))return json({error:"Challenge expired"},401);if(!await verifyWallet(message,signature,address))return json({error:"Invalid signature"},401);await db()`DELETE FROM auth_nonces WHERE address=${a}`;const session=await token({address:a,exp:Date.now()+7*86400_000});return new Response(JSON.stringify({ok:true}),{headers:{"content-type":"application/json","set-cookie":`rr_session=${encodeURIComponent(session)}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=604800`}})}catch{return json({error:"Verification failed"},400)}}
