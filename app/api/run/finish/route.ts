import {init,db,json,session,untoken} from "../../../../lib/server";
export async function POST(req:Request){try{const address=await session(req);if(!address)return json({error:"Unauthorized"},401);const b=await req.json()as{run:{id:string;token:string};durationMs:number;distance:number;gems:number;rekt:number;score:number};const p=await untoken(b.run.token);if(!p||p.id!==b.run.id||p.address!==address||Number(p.exp)<Date.now())return json({error:"Invalid run"},400);await init();const d=db();const [row]=await d<[{started_at:string;finished_at:string|null}]>`SELECT started_at,finished_at FROM runs WHERE id=${b.run.id} AND address=${address}`;if(!row||row.finished_at)return json({error:"Run already used"},409);const wall=Date.now()-Number(row.started_at),expected=Math.floor(b.distance+b.gems*120+b.rekt*200);const sane=Number.isInteger(b.score)&&b.score===expected&&b.durationMs>1500&&Math.abs(wall-b.durationMs)<15000&&b.distance>=0&&b.distance<=b.durationMs*.09&&b.gems>=0&&b.gems<=Math.ceil(b.durationMs/350)&&b.rekt>=0&&b.rekt<=Math.ceil(b.durationMs/400);await d`UPDATE runs SET finished_at=${Date.now()},duration_ms=${b.durationMs},distance=${b.distance},gems=${b.gems},rekt=${b.rekt},score=${b.score},valid=${sane?1:0} WHERE id=${b.run.id}`;if(sane)await d`INSERT INTO profiles(address,shards) VALUES(${address},${Math.floor(b.gems)}) ON CONFLICT(address) DO UPDATE SET shards=profiles.shards+EXCLUDED.shards`;return sane?json({ok:true,score:b.score}):json({error:"Run failed integrity checks"},422)}catch{return json({error:"Submission failed"},400)}}
