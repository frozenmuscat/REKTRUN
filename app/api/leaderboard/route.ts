import { init,db,json } from "../../../lib/server";
export async function GET(){try{await init();const rows=await db()<Array<{address:string;score:number;distance:number;gems:number;rekt:number}>>`SELECT address,MAX(score)::int score,MAX(distance)::int distance,MAX(gems)::int gems,MAX(rekt)::int rekt FROM runs WHERE valid=1 GROUP BY address ORDER BY score DESC LIMIT 100`;return json({scores:rows.map((x,i)=>({...x,rank:i+1}))})}catch{return json({scores:[]})}}
