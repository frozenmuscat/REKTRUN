let audio:AudioContext|null=null,master:GainNode|null=null,musicTimer:number|undefined,step=0,enabled=true;
const AudioCtor=()=>window.AudioContext||(window as typeof window&{webkitAudioContext:typeof AudioContext}).webkitAudioContext;
export function unlockAudio(){if(typeof window==="undefined"||!enabled)return;if(!audio){audio=new (AudioCtor())();master=audio.createGain();master.gain.value=.22;master.connect(audio.destination)}if(audio.state==="suspended")audio.resume()}
function tone(freq:number,duration=.12,type:OscillatorType="square",volume=.09,when=0){if(!audio||!master||!enabled)return;const o=audio.createOscillator(),g=audio.createGain(),t=audio.currentTime+when;o.type=type;o.frequency.setValueAtTime(freq,t);g.gain.setValueAtTime(0,t);g.gain.linearRampToValueAtTime(volume,t+.012);g.gain.exponentialRampToValueAtTime(.0001,t+duration);o.connect(g);g.connect(master);o.start(t);o.stop(t+duration+.02)}
export function startMusic(){unlockAudio();if(musicTimer||!audio)return;const notes=[110,146.83,164.81,196,164.81,146.83,123.47,146.83,110,146.83,220,196,164.81,146.83,123.47,98];musicTimer=window.setInterval(()=>{if(!enabled)return;const n=notes[step++%notes.length];tone(n,.19,"triangle",.055);if(step%4===1)tone(n/2,.34,"sine",.045);if(step%8===5)tone(n*2,.08,"square",.025)},190)}
export function stopMusic(){if(musicTimer){clearInterval(musicTimer);musicTimer=undefined}}
export function setAudioEnabled(value:boolean){enabled=value;if(value){unlockAudio();if(master&&audio)master.gain.setTargetAtTime(.22,audio.currentTime,.02);startMusic()}else{stopMusic();if(master)master.gain.setTargetAtTime(0,audio?.currentTime||0,.02)}}
export function sfx(kind:"slash"|"shot"|"hit"|"hurt"|"pickup"|"boss"|"dash") {unlockAudio();if(kind==="slash"){tone(520,.06,"sawtooth",.11);tone(240,.12,"square",.06,.035)}if(kind==="shot"){tone(760,.1,"square",.08);tone(380,.16,"sawtooth",.05,.04)}if(kind==="hit"){tone(90,.11,"square",.13);tone(180,.08,"sawtooth",.08)}if(kind==="hurt"){tone(70,.24,"sawtooth",.13)}if(kind==="pickup"){tone(660,.08,"square",.07);tone(990,.13,"square",.06,.07)}if(kind==="boss"){tone(55,.5,"sawtooth",.14);tone(82.4,.45,"square",.08,.08)}if(kind==="dash"){tone(280,.08,"sawtooth",.06);tone(560,.1,"triangle",.05,.035)}}
